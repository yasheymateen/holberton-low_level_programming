#include <stdio.h>
/**
 * main - function that prints all single digit integers from 0
 * Return: void
 */

int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
